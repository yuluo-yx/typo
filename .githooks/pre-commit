#!/usr/bin/env sh
set -eu

if ! command -v golangci-lint >/dev/null 2>&1; then
  echo "golangci-lint not found. Install it before committing."
  exit 1
fi

echo "Running golangci-lint before commit..."
golangci-lint run ./... --config ./tools/linter/.golangci.yml
